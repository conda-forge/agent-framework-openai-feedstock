schema_version: 1

context:
  version: "1.8.0"
  python_min: "3.10"

package:
  name: agent-framework-openai
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/a/agent-framework-openai/agent_framework_openai-${{ version }}.tar.gz
  sha256: 4397aa6f2ba4b630e8190fb3293ffdee0b1a08164b2cf36385a3d7afbf5f96ab

build:
  noarch: python
  number: 0
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - flit-core <4.0,>=3.11
  run:
    - python >=${{ python_min }}
    - agent-framework-core <2,>=1.4.0
    - openai <3,>=1.99.0

tests:
  - python:
      imports:
        - agent_framework.openai
      python_version: ${{ python_min }}.*
      pip_check: true

about:
  summary: |
    OpenAI integrations for Microsoft Agent Framework.
  homepage: https://pypi.org/project/agent-framework-openai/
  repository: https://github.com/microsoft/agent-framework
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - killua156
